# ไฟล์นี้คือ "หัวใจการเชื่อมต่อฐานข้อมูล" ครับ
# ทำไมถึงมีไฟล์นี้? -> เพื่อสร้างตัวแปร `db` ที่เป็นตัวกลางใช้คุยกับฐานข้อมูล SQL ทั่วทั้งโปรเจกต์
# ที่เขียนแยกออกมาแบบนี้ เพื่อป้องกันการเกิด Circular Import (การเรียกใช้งานวนลูป) ระหว่างไฟล์ App หลักกับไฟล์ Models ครับ

from flask_sqlalchemy import SQLAlchemy

db = SQLAlchemy()
