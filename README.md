# 🏭 Assembly Line Part Tracking System

**ระบบติดตามชิ้นส่วนในสายการผลิต**

คู่มือนี้สำหรับนักพัฒนาที่ต้องการติดตั้งและรันโปรเจกต์ในเครื่องตัวเอง (Local Setup)

---

## 🛠️ ขั้นตอนการติดตั้ง (Installation)

### 1. การเตรียมไฟล์

Clone โปรเจกต์นี้ลงในเครื่องของคุณ:

```bash
git clone <repository-url>
cd assembly-line-part-tracking
```

### 2. สร้าง Virtual Environment

เพื่อป้องกัน Library ตีกับโปรเจกต์อื่น:

```bash
# บน Windows teminal cmd
python -m venv venv
venv\Scripts\activate

# บน Mac/Linux teminal
python3 -m venv venv
source venv/bin/activate
```

### 3. ติดตั้ง Dependencies

ติดตั้ง Library ทั้งหมดที่จำเป็น:

```bash
pip install -r requirements.txt
```

### 4. ตั้งค่าตัวแปรระบบ (Environment Variables)

1. คัดลอกไฟล์ `.env.example` แล้วเปลี่ยนชื่อเป็น `.env`
2. แก้ไขข้อมูลใน `.env` ให้ตรงกับ MySQL/TiDB ของคุณ (หากไม่มีการแก้ไข ระบบจะใช้ SQLite เป็นไฟล์สำรองให้โดยอัตโนมัติ)

---

## 🚀 เริ่มรันระบบ

เมื่อตั้งค่าทุกอย่างเสร็จแล้ว สั่งรันด้วยคำสั่ง:

```bash
python run.py
```

เข้าใช้งานผ่านเบราว์เซอร์ที่: `http://localhost:5000`

---

## username password ที่ใช้ในการทดสอบ

-- admin
--- admin@example.com admin123
-- operator
--- operator@example.com op123

---

## 📖 แหล่งข้อมูลเพิ่มเติม

- **เจาะลึกการทำงานของโค้ด (ภาษาไทย):** [คลิกอ่านที่นี่ -> อธิบายระบบ.md]
- **โครงสร้างโฟลเดอร์:** ดูสรุปหัวไฟล์แต่ละไฟล์ได้โดยตรงในโค้ด (มีคอมเมนต์ภาษาไทยกำกับไว้ให้แล้วครับ)
