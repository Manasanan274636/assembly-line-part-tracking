# อธิบายระบบแบบเข้าใจง่าย

ไฟล์นี้เขียนขึ้นมาเพื่ออธิบายว่าในโปรเจคนี้มีโฟลเดอร์อะไรบ้าง แต่ละส่วนเก็บอะไร และทำหน้าที่อะไรในระบบ  
จะอธิบายแบบภาพรวม อ่านแล้วควรพอเข้าใจได้ทันทีว่าโครงสร้างโปรเจคนี้แบ่งงานกันอย่างไร

---

## ภาพรวมของระบบ

ระบบนี้คือเว็บแอปสำหรับบริหารชิ้นส่วนในไลน์ประกอบรถยนต์  
แนวคิดหลักของระบบคือ

- Admin จัดการข้อมูลตั้งต้น
- Admin สร้างแผนการผลิต
- Operator ดูแผนที่ต้องผลิต
- Operator บันทึกการใช้ชิ้นส่วนจริงและของเสีย
- ระบบนำข้อมูลไปอัปเดตสต็อกและสรุปเป็นรายงาน

ถ้าพูดให้สั้นที่สุด ระบบนี้ตอบคำถามหลัก 4 เรื่องคือ

- มีชิ้นส่วนอะไรในโรงงานบ้าง
- รถแต่ละโมเดลต้องใช้ชิ้นส่วนอะไรบ้าง
- วันนี้หรือช่วงนี้ต้องผลิตอะไรเท่าไร
- ใช้ชิ้นส่วนจริงไปเท่าไร เหลือเท่าไร และมีของเสียเท่าไร

---

## โครงสร้างโฟลเดอร์หลัก

### โฟลเดอร์ `app/`

โฟลเดอร์นี้คือหัวใจของระบบ  
เก็บโค้ดหลักทั้งหมดที่ใช้รันเว็บแอป

ข้างในจะแบ่งออกเป็นหลายส่วน เช่น

- `models` เก็บโครงสร้างข้อมูล
- `routes` เก็บการทำงานของแต่ละเมนู
- `templates` เก็บหน้าตาเว็บ
- `utils` เก็บฟังก์ชันช่วยเหลือ
- `static` เก็บไฟล์หน้าบ้าน เช่น css หรือ js

---

## อธิบายแต่ละโฟลเดอร์

### `app/models/`

โฟลเดอร์นี้เก็บ **โครงสร้างข้อมูลของระบบ**  
พูดง่าย ๆ คือเก็บว่าระบบมีตารางอะไรบ้างในฐานข้อมูล และแต่ละตารางมีข้อมูลอะไร

ตัวอย่างข้อมูลสำคัญที่อยู่ในโฟลเดอร์นี้

- `user.py` เก็บข้อมูลผู้ใช้งาน เช่น username, password, role
- `part.py` เก็บข้อมูลชิ้นส่วน เช่น รหัส part, ชื่อ part, stock, min stock
- `station.py` เก็บข้อมูลสถานีการผลิต
- `bom.py` เก็บข้อมูล BOM ของแต่ละโมเดลรถ
- `production_plan.py` เก็บแผนการผลิต
- `consumption.py` เก็บข้อมูลการใช้ชิ้นส่วนจริง
- `scrap.py` เก็บข้อมูลของเสีย
- `stock.py` เก็บข้อมูลสต็อก
- `claim.py` เก็บข้อมูลการเคลม
- `activity_log.py` เก็บประวัติการทำรายการต่าง ๆ

สรุปสั้น ๆ คือ  
`models` = ส่วนที่กำหนดว่า “ระบบเก็บข้อมูลอะไรบ้าง”

---

### `app/routes/`

โฟลเดอร์นี้เก็บ **logic การทำงานของระบบ**  
หรือก็คือเมื่อผู้ใช้กดเมนู กดปุ่ม submit หรือเปิดหน้าเว็บ ระบบจะทำอะไรต่อ

ไฟล์หลักในส่วนนี้คือ

- `auth.py` ดูแลเรื่อง login และ logout
- `admin.py` ดูแลเมนูฝั่ง admin
- `operator.py` ดูแลเมนูฝั่ง operator

ตัวอย่างหน้าที่ของ `routes`

- รับค่าจากฟอร์ม
- ตรวจสอบสิทธิ์ผู้ใช้
- ดึงข้อมูลจากฐานข้อมูล
- คำนวณข้อมูล
- ส่งข้อมูลไปแสดงที่หน้าเว็บ

สรุปสั้น ๆ คือ  
`routes` = ส่วนที่กำหนดว่า “ผู้ใช้ทำอะไรแล้วระบบตอบสนองอย่างไร”

---

### `app/templates/`

โฟลเดอร์นี้เก็บ **หน้าตาเว็บ หรือฝั่งหน้าบ้านของระบบ**  
เป็นส่วนของ UX/UI ที่ผู้ใช้มองเห็นจริง

เช่น

- หน้า login
- หน้า dashboard
- หน้า part management
- หน้า station management
- หน้า BOM
- หน้า production plan
- หน้า consumption
- หน้า report

ภายในโฟลเดอร์นี้จะแยกอีกเป็น

- `auth/` เก็บหน้าที่เกี่ยวกับการเข้าสู่ระบบ
- `admin/` เก็บหน้าฝั่ง admin
- `operator/` เก็บหน้าฝั่ง operator
- `base_admin.html` คือแม่แบบหลักของหน้าฝั่ง admin
- `base_operator.html` คือแม่แบบหลักของหน้าฝั่ง operator

สรุปสั้น ๆ คือ  
`templates` = ส่วนที่เก็บ **UX, UI และหน้าบ้านของระบบ**

---

### `app/static/`

โฟลเดอร์นี้เก็บไฟล์ประกอบหน้าบ้านที่ไม่ใช่ template โดยตรง เช่น

- CSS
- JavaScript
- asset ต่าง ๆ

หน้าที่คือช่วยเรื่องหน้าตา สี layout และพฤติกรรมของหน้าเว็บ

สรุปสั้น ๆ คือ  
`static` = ไฟล์ตกแต่งและช่วยให้หน้าเว็บแสดงผลสวยและใช้งานง่ายขึ้น

---

### `app/utils/`

โฟลเดอร์นี้เก็บ **ฟังก์ชันช่วยเหลือ** ที่ใช้ร่วมกันหลายจุดในระบบ

ตัวอย่างเช่น

- `db.py` ใช้เชื่อมต่อฐานข้อมูล
- `decorators.py` ใช้ตรวจสิทธิ์ เช่น ให้เฉพาะ admin เข้าเมนู admin ได้
- `time_utils.py` ใช้ฟังก์ชันเกี่ยวกับวันเวลา

สรุปสั้น ๆ คือ  
`utils` = เครื่องมือช่วยให้โค้ดส่วนอื่นทำงานได้สะดวกขึ้น

---

## ไฟล์สำคัญระดับบนสุดของโปรเจค

### `run.py`

ไฟล์นี้ใช้สำหรับรันระบบ  
เปรียบเหมือนปุ่มเริ่มต้นของโปรแกรม

### `app/__init__.py`

ไฟล์นี้ใช้ประกอบระบบทั้งหมดเข้าด้วยกัน เช่น

- โหลด config
- เชื่อมฐานข้อมูล
- ตั้งค่า login
- ลงทะเบียน routes

เป็นเหมือนจุดศูนย์กลางของการสร้างแอป

### `app/config.py`

ไฟล์นี้เก็บค่าตั้งค่าของระบบ เช่น

- secret key
- database config
- ค่าการตั้งค่าต่าง ๆ ของ Flask

### `requirements.txt`

เก็บรายชื่อไลบรารีที่ระบบต้องใช้  
เช่น Flask, SQLAlchemy, Pandas

### `init_db.py`

ใช้สำหรับสร้างฐานข้อมูลเริ่มต้น

### `seed_data.py`

ใช้สำหรับใส่ข้อมูลตัวอย่างลงในระบบ

---

## โฟลเดอร์อื่น ๆ

### `docs/`

เก็บเอกสารประกอบโปรเจค เช่นคู่มือหรือขอบเขตระบบ  
เป็นส่วนของเอกสาร ไม่ใช่โค้ดหลักของการรันเว็บ

### `migrations/`

เก็บไฟล์เกี่ยวกับการอัปเดตโครงสร้างฐานข้อมูล  
ใช้ตอนมีการเปลี่ยน table หรือ column ในระบบ

### `เอกสารโปรเจค/`

เก็บไฟล์เอกสารของโครงงาน เช่น PDF, Excel, เอกสารอ้างอิง และไฟล์ที่ใช้ประกอบรายงาน

### `venv/`

เก็บ environment ของ Python  
เอาไว้แยก package ของโปรเจคนี้ออกจากเครื่องหลัก

### `.idea/` และ `.vscode/`

เป็นไฟล์ตั้งค่าของโปรแกรม editor  
ไม่ได้เป็นส่วนสำคัญของ logic ระบบโดยตรง

---

## สรุปการไหลของระบบ

เพื่อให้เห็นภาพรวมง่าย ๆ ระบบนี้ทำงานประมาณนี้

1. Admin เข้ามาจัดการข้อมูลตั้งต้น เช่น part, station, user และ BOM
2. Admin สร้าง production plan ว่าจะผลิตโมเดลไหน จำนวนเท่าไร
3. ระบบใช้ BOM คำนวณว่าต้องใช้ part อะไรบ้างตามแผน
4. Operator เข้ามาดูแผนที่ต้องผลิต
5. Operator บันทึก consumption และ scrap ตามการใช้งานจริงที่ station
6. ระบบอัปเดต stock และนำข้อมูลไปแสดงใน dashboard กับ reports

---

## สรุปแบบสั้นที่สุด

ถ้าอยากเข้าใจโปรเจคนี้เร็วที่สุด ให้จำแบบนี้ได้เลย

- `models` = เก็บข้อมูลอะไร
- `routes` = ระบบทำงานอย่างไร
- `templates` = หน้าเว็บที่ผู้ใช้เห็น
- `static` = ไฟล์ตกแต่งหน้าบ้าน
- `utils` = ฟังก์ชันช่วยเหลือ
- `run.py` = ใช้รันระบบ

ดังนั้นเวลาอ่านโปรเจคนี้

- ถ้าอยากรู้ข้อมูลอยู่ตรงไหน ให้ดู `models`
- ถ้าอยากรู้ปุ่มนี้กดแล้วทำอะไร ให้ดู `routes`
- ถ้าอยากรู้หน้าตาอยู่ตรงไหน ให้ดู `templates`

---

## หมายเหตุ

ไฟล์นี้ตั้งใจเขียนให้เข้าใจง่ายก่อน  
ไม่ได้ลงลึกถึงโค้ดทุกบรรทัดหรือทุกฟังก์ชัน  
ถ้าต้องการอธิบายแบบละเอียดระดับรายไฟล์หรือรายเมนู สามารถแยกเขียนเพิ่มต่อได้ภายหลัง
