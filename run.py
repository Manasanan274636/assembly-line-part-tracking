# ไฟล์นี้คือ "ปุ่มเปิดเครื่อง" (Entry Point)
# ทำไมถึงมีไฟล์นี้? -> เป็นไฟล์หลักที่คุณต้องสั่งรัน (python run.py) เพื่อให้เว็บไซต์เริ่มทำงาน
# หน้าที่ของมันสั้นมากคือ: ไปดึงหน้าตาเว็บมาจากโฟลเดอร์ app แล้วสั่ง "Run" เปิดเซิร์ฟเวอร์จำลองขึ้นมาครับ

from app import create_app

app = create_app()

if __name__ == "__main__":
    app.run(debug=True)
